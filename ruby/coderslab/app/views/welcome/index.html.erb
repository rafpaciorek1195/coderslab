
hello world

<%= link_to 'Powitanie', main_path %>
<br>
<%= link_to 'Root', root_path %>

<table>
  <tr>
    <th>ID</th>
    <th>Nazwa</th>
    <th>Stworzona</th>
    <th>Aktualizowana</th>
    <th>Opis</th>
  </tr>
  <% @paths.each do |r| %>
    <tr>
    <td> <%= r.id %> </td>
    <td> <%= r.path_name %> </td>
    <td> <%= r.created_at %>v</td>
    <td> <%= r.updated_at  %>/td> 
    <td> <%= r.description %> </td>
    </tr>
  <% end %>
  </table>

  <h1>
<% @paths.each do |t| %>
<%= t.desc %>
<br>
<% end %>
  </h1>
  <%= @paths.first.check %>



